import getFoci from 'dotplot/utils/get-foci';
import { module, test } from 'qunit';

module('Unit | Utility | get foci');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getFoci();
  assert.ok(result);
});
