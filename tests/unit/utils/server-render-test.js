import serverRender from 'dotplot/utils/server-render';
import { module, test } from 'qunit';

module('Unit | Utility | server render');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = serverRender();
  assert.ok(result);
});
